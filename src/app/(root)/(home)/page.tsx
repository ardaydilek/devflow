import { ModeToggle } from "@/components/shared/navbar/mode-toggle";
import { UserButton } from "@clerk/nextjs";

export default function Home({}) {
  return (
    <div>
      <h1 className="text-red-500 dark:text-blue-500">Home</h1>
    </div>
  );
}
